{
  "mcpServers": {
    "sama-data-prep-mcp-server": {
      "command": "sama-data-prep-mcp-server",
      "disabled": false,
      "autoApprove": [],
      "description": "SAMA Data Preparation Server - Dataset preparation and formatting for fine-tuning"
    },
    "sama-deployment-mcp-server": {
      "command": "sama-deployment-mcp-server", 
      "disabled": false,
      "autoApprove": [],
      "description": "SAMA Deployment Server - SageMaker model deployment and serving"
    }
  },
  "usage_examples": {
    "data_preparation": [
      "Inspect the databricks/databricks-dolly-15k dataset",
      "Prepare databricks/databricks-dolly-15k for instruction tuning",
      "Prepare Dolly dataset filtering for summarization category only",
      "Do a full data prep workflow on databricks/databricks-dolly-15k",
      "Create instruction template with custom field names",
      "Upload training data to S3 with prefix 'my-finetune-data'"
    ],
    "full_iteration": [
      "Let's do a full iteration on databricks/databricks-dolly-15k",
      "Complete ML workflow for CNN/DailyMail summarization dataset",
      "Full pipeline from data prep to deployment for SQuAD dataset"
    ],
    "integration": [
      "After data prep, deploy meta-textgeneration-llama-3-2-3b model",
      "Prepare data then start fine-tuning job",
      "Complete workflow: prep data, fine-tune, deploy, evaluate"
    ]
  },
  "workflow_coordination": {
    "description": "When user requests a 'full iteration', the servers should coordinate in this order:",
    "steps": [
      {
        "step": 1,
        "server": "sama-data-prep-mcp-server",
        "action": "Prepare and upload dataset",
        "tools": ["full_data_prep_workflow", "upload_training_data_to_s3"]
      },
      {
        "step": 2, 
        "server": "sama-finetuning-mcp-server",
        "action": "Start fine-tuning job (future)",
        "tools": ["create_training_job", "monitor_training_job"]
      },
      {
        "step": 3,
        "server": "sama-deployment-mcp-server", 
        "action": "Deploy fine-tuned model",
        "tools": ["deploy_jumpstart_model", "create_inference_payload"]
      },
      {
        "step": 4,
        "server": "sama-evaluation-mcp-server",
        "action": "Evaluate model performance (future)",
        "tools": ["run_evaluation", "generate_metrics"]
      }
    ]
  }
}
